import 'package:flutter/material.dart';

class RitualDescriptionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Ritual Details'),
      ),
      body: Center(child: Text('Ritual Description'),),
    );
  }
}